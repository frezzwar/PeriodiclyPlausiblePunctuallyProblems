/* This file was generated by SableCC (http://www.sablecc.org/). */

package compiler.node;

import java.util.*;
import compiler.analysis.*;

@SuppressWarnings("nls")
public final class AParams extends PParams
{
    private TIdentifier _identifier_;
    private final LinkedList<PParamsTail> _paramsTail_ = new LinkedList<PParamsTail>();

    public AParams()
    {
        // Constructor
    }

    public AParams(
        @SuppressWarnings("hiding") TIdentifier _identifier_,
        @SuppressWarnings("hiding") List<?> _paramsTail_)
    {
        // Constructor
        setIdentifier(_identifier_);

        setParamsTail(_paramsTail_);

    }

    @Override
    public Object clone()
    {
        return new AParams(
            cloneNode(this._identifier_),
            cloneList(this._paramsTail_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAParams(this);
    }

    public TIdentifier getIdentifier()
    {
        return this._identifier_;
    }

    public void setIdentifier(TIdentifier node)
    {
        if(this._identifier_ != null)
        {
            this._identifier_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._identifier_ = node;
    }

    public LinkedList<PParamsTail> getParamsTail()
    {
        return this._paramsTail_;
    }

    public void setParamsTail(List<?> list)
    {
        for(PParamsTail e : this._paramsTail_)
        {
            e.parent(null);
        }
        this._paramsTail_.clear();

        for(Object obj_e : list)
        {
            PParamsTail e = (PParamsTail) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._paramsTail_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._identifier_)
            + toString(this._paramsTail_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._identifier_ == child)
        {
            this._identifier_ = null;
            return;
        }

        if(this._paramsTail_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        if(this._identifier_ == oldChild)
        {
            setIdentifier((TIdentifier) newChild);
            return;
        }

        for(ListIterator<PParamsTail> i = this._paramsTail_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PParamsTail) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
