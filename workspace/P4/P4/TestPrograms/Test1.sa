	////////////////////////////////////////
	/*	Declaration of global functions	  */
	////////////////////////////////////////
test(variable){
	return variable;
}
test1(variable, variable1){
	return variable + variable1;
}
test2(variable){

}

main(){
	////////////////////////////////////////
	/*	Declaration of variable			  */
	////////////////////////////////////////

	// Multiple declaration of variables
	new variable_1 = 15, new variable_2 = "15", new variable_n = true;
	
	// Single declaration of a variable
	new variabl = "Single variable";
	
	// Multiple declaration of list variables
	new list_variable_1 = [5], new list_variable_2 = [1];
	
	// Single declaration of a list variable
	new list_variable = [1];
	
	// Mix of single and list
	new var1 = ["2", true], new var2 = 2, new var3 = [true, 1];

	// Single declaration of a variable assigned with a function
	new variable11 = number(4,5);

	////////////////////////////////////////
	/*	Assign variable 				  */
	////////////////////////////////////////
	
	// Assign single variable
	a = 5;
	
	// Assign variable within two objects
	b.c.d = 5;
	
	// Assign single variable with function
	a = number(2,3);
	
	// Assign variable within one object with a function
	b.c = number(7,5);
	
	// Assign single variable with an expression
	a = b * 2 / 4 * 5;
	
	// Assign derp with a shitload of logic
	derp = true == true || 45 == 50 && 5 != false || true == 45.15 && b.e.d + 6 != 1;
	
	////////////////////////////////////////
	/*	Control statements				  */
	////////////////////////////////////////
	
	// if followed by a else statement
	if (true) {}
	else {}
	
	// if followed by a else if and a else statement
	if (true) {}
	else if (true) {}
	else {}
	
	// while statement
	repeat while (true) {}
	
	// repeat statements
	repeat (random (1 to 5)) {}
	repeat (10, 10) {}
	
	// foreach statements
	repeat foreach (body_variable in test.name){}
	repeat foreach (body_variable in listofnames){}
	
	
}





